# Data

This directory is reserved for manuscript-owned data artifacts.

Create purpose-specific subdirectories such as `raw/`, `processed/`, or `derived/` only when real repo-owned data exists. Do not keep empty placeholder trees.
